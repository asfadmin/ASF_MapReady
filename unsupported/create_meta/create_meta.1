
NAME:	      create_meta

SYNOPSIS:     create_meta <in> <out>

DESCRIPTION:

	   in: a CEOS, AISP, or other metadata input file
	  out: a .meta file, containing the essentials
	       distilled from above.
	
	    create_meta analyses the given input file, 
	 and attempts to extract all the metadata it needs.
	 It will create a valid .meta file, or report an error.
	 The .meta file it creates is an ASCII file containing
	 all metadata required for geocoding, interferometric
	 processing, and other ASF software.


EXAMPLES:

To convert the metadata in a CEOS file E1203.D and E1203.L 
into the metadata file new_e1.meta:

	> create_meta E1203 new_e1

To convert the image data as well, use "sarin".

CURRENTLY SUPPORTED FORMATS:
ASF Data Center:
	Pre-RADARSAT and Post-RADARSAT products;
	ERS-1/2, JERS, RADARSAT Standard, RAMP, and ScanSAR


SEE ALSO:
	sarin

AUTHOR:	  O. Lawlor, 1998

CONTACT:  http://www.images.alaska.edu/
          rguritz@images.alaska.edu
