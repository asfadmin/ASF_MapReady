#===============================================================================
# Filename:	faif_makedefs
# Description:	Standard makedefs for all FAIF makefiles	
# Creator:	Norbert Piega (Based on aps_makedefs by R.Green)
# Notes:	
#	This is the standard makedefs for all FAIF makefiles.
# This should be present in the top src directory and should be
# included in each makefile using relative paths.
#
# 1.  Dec. 97 - RHoffman
#     Changing linker options on SYBASE-related libraries:
#     Omit -R$(SYBASE)/lib
#
# SCCS Info:
#  faif_makedefs
#  1
#  12/03/97
#  @(#)faif_makedefs	1.3
#===============================================================================

# Machine independant environment variables.
# We always use Sparc for the moment - if that changes, we should get
# this from an environment variable
MACH=sparc
OS = solaris
#CC=/opt/SUNWspro/bin/cc
CC = cc

# We always use gnumake.
MAKE=gnumake
LINT=lint -ux

# Linker options.
LD=ld
LDOPTS=-G
AR=ar rucv
RANLIB=echo

# Machine-specific flags.
sparcCFLAGS=-Xc
sparcLFLAGS=-L/usr/ccs/lib
sparcLLIBS=-lsocket -lnsl -lgen
sparcRPCFLAGS=

#sparcLLIBS=-Bdynamic -lsocket -lgen -lnsl -lintl  -ldl -lc -lucb
#MATH_LIB=-Bdynamic -lm 
#SPARC_LIB=-Bdynamic -lnsl -lcrypt -lsocket -lgen -lelf -lc -lucb
MATH_LIB=-R/opt/SUNWspro/lib -lF77 -lM77 -lsunmath -lm
#SPARC_LIB=-R/usr/lib -Bdynamic -lnsl -lsocket -lgen -lelf -lc
SPARC_LIB=-R/usr/lib -Bdynamic -lnsl -lsocket -lgen -lelf -ldl -lc


# Machine independant flags and environment variables.

# Compile flags.  Debug (-g).  Profile (-pg).  Optimize (-O)
DPOFLAGS=-g

# Link flags.
DYNAMIC=-Bdynamic
STATIC=-Bstatic

# Environment variables for use in makefiles.
MACHCFLAGS=$(DPOFLAGS) $($(MACH)CFLAGS)
MACHLFLAGS=$($(MACH)LFLAGS)
MACHLLIBS=$($(MACH)LLIBS)
MACHRPCFLAGS=$($(MACH)RPCFLAGS)

# CM (Configuration Management) flags and environment variables
CM=sccs
CM_GET=get

# 3rd Party Application flags and environment variables.

# X Windows / Motif
IXMOTIF=-I/usr/include -I/opt/SUNWmotif/include 
LXMOTIF=-L/usr/lib/X11 -L/opt/SUNWmotif/lib 
XMOTIF_LIB=-Bdynamic -lXm -lXt -lX11

# ViewCenter 
UX_DIR=/usr/local/CenterLine/vctrm/sparc-solaris2
IVCENTER=-I$(UX_DIR)/include
LVCENTER=-L$(UX_DIR)/lib
VCENTER_LIB= 

# Sybase 
# Assumes environment variable SYBASE is set
ISYBASE=-I$(SYBASE)/include 
LSYBASE=-L$(SYBASE)/lib
SYBASE_LIB=-Bstatic -lsybdb 

# Solaris Encryption 
IENCRYPT=-I/usr/include/rpc
LENCRYPT=-L/usr/lib
#ENCRYPT_LIB=-Bdynamic -lcrypt
ENCRYPT_LIB=-lcrypt

# Sunlink FTAM
FTAM=/opt/SUNWconn/ftam
IFTAM=-I$(FTAM)/include
LFTAM=-I$(FTAM)/lib
FTAM_LIB=-lnsl -lintl -lftam

# DCE
DCE_IDL=idl
DCE_IDLFLAGS=-v
IDCE=-I/opt/dce/share/include/dce -I/opt/dce/share/include
DCE_LIB=-Bdynamic -ldce -Bstatic -lsocket -Bdynamic -lnsl -lthread -Bstatic -lm
DCE_CFLAGS=-D_REENTRANT

# Lex and Yacc
LEX=lex
YACC=yacc
YFLAGS=-d


# ASF System specific environment variables.
# ASF{ROOT,LIB,INC,BIN} environment variable creation
#ASFROOT=/asf
ASFROOT=/ASF/$(ASFVER)
ASFLIB=$(ASFROOT)/lib/$(MACH)
ASFINC=$(ASFROOT)/include
ASFBIN=$(ASFROOT)/bin

# Directories for global FAIF files to be stored in
# ASF system directories
FAIFINC=$(ASFINC)/faif
FAIFLIB=$(ASFLIB)
FAIFBIN=$(ASFBIN)

# IMS directories
IMSINC=$(ASFINC)/imsdads
IMSLIB=$(ASFLIB)
IMSBIN=$(ASFBIN)

# APS directories
APSINC=$(ASFINC)/aps
APSLIB=$(ASFLIB)
APSBIN=$(ASFBIN)

# If you have to use UCBLIB, use this.
# Note that -lc (libc) is put in front to prevent libc vs. ucb clash
UCB_LIB=-Bstatic -lc -L/usr/ucblib /usr/ucblib/libucb.a
